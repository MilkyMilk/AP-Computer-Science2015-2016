import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class SuperFood here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class SuperFood extends Food
{
    // private boolean top;
    // private boolean bottom;
    // private boolean left;
    // private boolean right;
    
    // public SuperFood() {
        // top = false;
        // bottom = false;
        // left = false;
        // right = false;
        // }
    
    /**
     * Act - do whatever the SuperFood wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // checkKeyPress();
        // move(1);
        // if (top) 
            // setLocation(getX(), 0);
            
        // else if (bottom) 
            // setLocation(getX(), getWorld().getHeight());
            
        // else if (left) 
            // setLocation(getWorld().getWidth() ,getY());
            
        // else if (right) 
            // setLocation(0, getY());
            
        
     }
    
     // /**
     // * Check whether a control key on the keyboard has been pressed.
     // * If it has, react accordingly.
     // */
    // public void checkKeyPress()
    // {
        // if (Greenfoot.isKeyDown("left")) 
        // {
            // setRotation(180);
        // }
        // if (Greenfoot.isKeyDown("right")) 
        // {
            // setRotation(0);
        // }
        // if (Greenfoot.isKeyDown("down")) 
        // {
            // setRotation(90);
        // }
        // if (Greenfoot.isKeyDown("up")) 
        // {
            // setRotation(270);
        // }
    // }
    
    // /**
     // * Test if we are close to one of the edges of the world. Return true if we are.
     // */
    // //@Override
    // public void atWorldEdge()
    // {
        // int rotation = getRotation();
        // if (rotation == 0 && getX() + 1 == getWorld().getWidth()) {
            // right = true;
            // }
        // else if (rotation == 180 && getX() == 0) {
            // left = true;
            // }
        // else if (rotation == 90 && getY() + 1 == getWorld().getHeight()) {
            // top = true;
            // }
        // else if (rotation == 270 && getY() == 0) {
            // bottom = true;
            // }
        // else {
            // right = false;
            // left = false;
            // top = false;
            // bottom = false;
            // }
    // }
}
